# PII redaction — Python dependencies
#
# Target: Python 3.11 (developed and verified on 3.11.9).
# Install:
#     python -m venv venv && source venv/bin/activate
#     pip install -r requirements.txt
#
# Versions are pinned to the environment the reported results were produced on.

# --- Model loading and inference (GLiNER2 + Transformers) ---
gliner2==1.3.1
transformers==5.1.0
torch==2.12.0
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub==1.18.0

# --- LoRA fine-tuning ---
peft==0.19.1
accelerate==1.13.0

# --- Rule-based recognizers (Presidio analyzer + spaCy NER) ---
presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362
spacy==3.8.14

# The spaCy English model powers the rule-based PERSON (full name) recognizer.
# It is not published on PyPI, so it is installed from its release wheel.
# Equivalent to: python -m spacy download en_core_web_lg
https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl

# --- Data handling and loss-curve plotting ---
pandas==3.0.3
numpy==2.4.6
matplotlib==3.11.1

# --- Residual-PII leak-judge notebook only (Azure o4-mini via LangChain) ---
# Needed just to run inference/leak_tests/residual_pii_analysis.ipynb; the
# benchmark and redaction pipeline do not depend on these.
langchain-openai==0.3.35
langchain-core==0.3.79
